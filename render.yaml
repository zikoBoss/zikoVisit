services:
  - type: web
    name: freefire-visit-bot
    runtime: python
    region: singapore
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn v2_termux:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0